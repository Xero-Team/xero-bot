services:
  xero-bot:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - XERO_DATA_DIR=/data
    volumes:
      - xero-data:/data
      - ./xero-review-bot.pem:/keys/bot.pem:ro
    restart: unless-stopped

volumes:
  xero-data:
